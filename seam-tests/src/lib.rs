//! Deliberately empty. This crate is `publish = false` and exists only to host
//! `tests/` and `examples/` that need both `linq_rs` and `linq_rs_sql` in
//! scope — see the manifest, and `DECISIONS.md` `D-024`.
